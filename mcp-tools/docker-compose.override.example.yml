# Example override file for local development
# Copy this to docker-compose.override.yml and customize

version: '3.8'

services:
  balance:
    environment:
      - DEBUG=true
    volumes:
      - ./balance:/app

  transfer:
    environment:
      - DEBUG=true
    volumes:
      - ./transfer:/app

